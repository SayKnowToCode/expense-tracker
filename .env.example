# Copy this file to .env and adjust as needed.

# SQLite is the default; the file is created on first migration.
DATABASE_URL="file:./prisma/dev.db"

# Backend HTTP port (optional, defaults to 4000)
PORT=4000
